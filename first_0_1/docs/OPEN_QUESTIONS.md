# 待确认

- 用户已提供 AJ-SR04M 手册，6 页全文及页面图表已核对：防水描述和 340m/s、Echo_us/58 换算没有提供水下测距保证。三个探头完全浸水的安装方案仍未具备可用的测距证据，需要明确改为空气中测距，或提供适用水下的硬件及验证结果。
- 用户已确认 K230 摄像头能看到滤网入口。仍需 K230 识别程序路径、图像分辨率、入口左右边界及入口边缘的像素坐标；当前只有横向居中追踪，未实现“已入网”确认。看到入口不等于能持续看到垃圾通过入口，需要用实际画面验证遮挡情况。
- 只有左右水平推进器，无法主动保持俯仰/横滚水平；若后续要求主动调平，需要可产生相应力矩的执行机构。
- 25°/200ms、45°及12°限推力是用户接受的初始阈值，尚未在实船波浪、振动和负载条件下验证。


- 2026-09-11：已实现 MPU 启动校验和校准诊断；数据持续为零的实际原因及电机能否转动仍未验证。
- 已确认 OLED 引脚为 GND/VDD/SCK/SDA。当前引脚方案让 OLED 与 MPU6050 共用 PB8/PB9 硬件 I2C1，并将 PB10/PB11 留给 USART3 调试。固件按 SSD1306 128×64 屏幕编写；实际控制器型号、上拉电阻、屏幕显示效果及实机时序仍未验证。第二块 TB6612 已改用于双线直流云台电机；在定义云台控制协议前，PA5 保持低电平。
- 2026-09-11：观察到 OLED 一直停留在最初的 MPU STARTING 页面，同时两个推进电机均未转动。已加入启动阶段显示诊断；烧录新 HEX 后显示的阶段或错误信息仍待实机观察。
- 2026-09-11：烧录带启动阶段诊断的版本后，据反馈 OLED 先显示原启动页面，随后黑屏。已放慢软件 I2C 速度，并改为显示写入失败后重试，重连时不再先发送关闭显示命令。OLED VDD 实际电压、控制器型号、板载上拉电阻，以及烧录此修复版本后的结果仍未验证。
- 2026-09-11：PB12/PB13 和 PB14/PB15 均接入时，推进电机不转；任意一组中拔掉一根线，对应电机就能转动，基于 MPU 的停车逻辑也有一定效果。STM32 到 D153C 端子的准确对应关系及 AIN/BIN 实测电压仍未确认。固件现可显示 PB12～PB15 输出值和读回值的四位状态，供诊断使用。
- 2026-09-11：曾测试仅驱动正向的临时台架方案：PB12/PB14 作为推挽驱动输出，PB13/PB15 作为下拉输入。反馈现象没有变化，因此已停用该方案；PB12～PB15 现均恢复为推挽输出。
- 2026-09-11：单线临时方案未改变反馈现象，因此加入上电复位时的方向诊断：两路先以 10 状态运行 1.5 秒，停 0.75 秒，再以 01 状态运行 1.5 秒，然后停车并开始正常的 MPU 初始化。仍需观察四根方向线全部接入时的结果，以区分固件烧录问题与 D153C 端子或接线故障。
- 2026-09-11：四根方向线全部接入时，方向诊断未使任一电机转动。用户随后更正关键现象：将标为 PB15 的线拔下后，接到 STM32 任意引脚，电机都会转动。由于这一现象与所接引脚无关，当时首先需要验证 Blue Pill、D153C 和电机电池之间是否缺少公共地或公共地连接不可靠。用户后来确认了接线标签。
- 2026-09-11：用户提供了完整接线照片。照片可确认推进驱动板使用右侧信号排针，但交叉的导线遮住了排针标识和 PB14/PB15 导线的端点。照片不足以证明引脚与端子的对应关系；仍需仅拍摄驱动板排针、清晰显示导线颜色及丝印的近照，或提供导线颜色与端子标识的文字对照表。
- 2026-09-11：用户确认 D153C 信号线接线正确，因此已停用临时单线方案，PB12～PB15 恢复为推挽方向输出。复位时方向诊断的 PWM 值由 2000 提高至 7199，因为此前成功的定速测试使用满占空比，而 2000 可能低于带载电机的启动阈值。烧录此版本后的硬件表现仍未验证。
- 2026-09-11：断电通断测试已确认 Blue Pill GND、D153C GND 和电机电池负极互相连通，因此已排除未共地。用户反馈，将任意一根方向线移接到 MCU 任意引脚，电机仍会转动；此时 TB6612 输入处于未受控或默认状态，不能据此确认所命令的逻辑电平。仍需在通电状态下，同时测量 D153C 的 AIN1/AIN2/BIN1/BIN2/PWM/STBY 端子电压。
- 2026-09-11：OLED 使用原 PB10/PB11 软件 I2C 接线时仍为黑屏。当前设计已将其移至与 MPU6050 共用的 PB8/PB9 硬件 I2C 总线；控制器型号、上拉电阻及新接线的实机结果仍未验证。进一步修改显示代码前，应读取 g_debugOledStatus、g_debugOledAddress 和 g_debugOledNoAckCount。
- 2026-09-11：用户反馈 PC13 板载 LED 由快闪变为慢闪。当前固件在约三秒的 INIT 校准阶段有多个逻辑同时控制 LED，另有间隔 200 毫秒的 MPU 启动故障循环，因此仅凭闪烁现象无法判断状态。需读取 g_debugBootStage、g_debugRunTick、g_debugState 和 g_debugImuStopReason，以区分正常校准与复位或启动故障。
